#include <stdio.h>

/**
 * main - This program uses the put function from
 * the standard library to print the defined
 * message to the standard output.
 *
 * Return: 0
 */
#include <stdio.h>
int main(void)
{
	char *msg;

	msg  = "\"Programming is like building a multilingual puzzle";
	puts(msg);
	return (0);
}
